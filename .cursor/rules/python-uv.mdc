---
description: 使用 uv 管理 Python 项目、环境、依赖、临时 CLI 工具和 Python 版本。
globs:
  - "**/*.py"
  - "pyproject.toml"
  - "uv.lock"
  - ".python-version"
alwaysApply: true
---

# Python uv 规则

## 运行环境

- 默认使用 `uv` 管理 Python 项目、虚拟环境和依赖。
- 运行项目内的 Python 代码时使用 `uv run`。
- 运行不需要长期加入项目依赖的临时 Python CLI 工具时使用 `uvx`。
- 当存在等价的 `uv` 或 `uvx` 命令时，不要直接调用 `python`、`python3`、`pip` 或 `pipx`。

## 依赖管理

- 使用 `uv add <package>` 添加运行时依赖。
- 使用 `uv add --dev <package>` 添加开发依赖。
- 默认安装最新兼容版本。除非项目因为兼容性或可复现性明确需要固定版本，否则不要手动锁死具体版本。
- 初始化项目后，依赖元数据保存在 `pyproject.toml`，解析后的锁文件保存在 `uv.lock`。

## Python 版本

- 使用 `.python-version` 中声明的 Python 版本。
- 创建或更新版本文件时，默认选择最新稳定版 Python。
- 截至 2026-06-01，最新稳定版 Python 为 `3.14.5`。

## 常用命令

```sh
uv run python -m py_uv_uvx_py_uv_add
uv run pytest
uvx ruff check .
uv add requests
uv add --dev pytest ruff
```
